spring:
  application:
    name: myspringboot
  output:
    ansi:
      enabled: always
  profiles:
    active: dev
  thymeleaf:
    encoding: UTF-8
    prefix: classpath:/templates/
    mode: HTML
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    password: 1214
    url: jdbc:mysql://192.168.3.195:3306/learn?serverTimezone=UTC&useUnicode=true&characterEncoding=utf-8&useSSL=true
    username: muwentao
# MyBatis配置
mybatis-plus:
  # 搜索指定包别名
  typeAliasesPackage: com.mwt.demo.entity
  # 配置mapper的扫描，找到所有的mapper.xml映射文件
  mapperLocations: classpath*:*/mapper/SysUserMapper.xml
  # 加载全局的配置文件
  configLocation: classpath:mybatis/mybatis-config.xml



server:
  tomcat:
    uri-encoding: UTF-8
    max-connections: 500
    min-spare-threads: 25
    max-threads: 300
    accept-count: 200
  port: 8081
com:
  mwt:
    title: mwt
    description:  this is  learn springboot way


#thymeleaf
logging:
  file:
    path: D:/log

